export type InputState = {
    username: string;
    password: string;
}

export type RegisterState = {
    username: string;
    password: string;
    company: string;
    rnc: string;
    email: string;
}

export type InputStatus = {
    hadValue: boolean;
    type: string;
}